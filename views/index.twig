{% extends 'layout.twig' %}

{% block body %}

    <div class="container">
        <h1>BlogModule</h1>

        {% for article in articles %}
           <ul>
               <li>
                   <a href="{{ path('BlogModule.show', {'slug': article.slug}) }}">{{ article.name }}</a>
                   <small>{{ article.category.name }}</small>
               </li>
           </ul>
        {% endfor %}
    </div>

{% endblock %}
